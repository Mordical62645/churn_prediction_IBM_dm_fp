To activate the virtual environment:
    in terminal run: 
        .venv/Scripts/activate

If there are problems, create a virtual environtment and install some packages/libraries:
    in terminal run: 
        py -m venv .venv
    
    after .venv is created run this to install the packages/libraries:
        pip install -r requirements.txt

        